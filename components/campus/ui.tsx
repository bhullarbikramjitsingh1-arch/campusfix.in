'use client';
import {Select,SelectTrigger,SelectValue,SelectContent,SelectItem} from '@/components/ui/select';
import {FileText,Clock3,LoaderCircle,CheckCircle2} from 'lucide-react';
import type {Report} from '@/lib/campus/model';
export function Choice({label,value,options,onChange}:{label:string;value:string;options:readonly string[];onChange:(v:string)=>void}){return <Select value={value} onValueChange={onChange}><SelectTrigger aria-label={label} className="choice"><SelectValue placeholder={label}/></SelectTrigger><SelectContent>{options.map(o=><SelectItem key={o} value={o}>{o}</SelectItem>)}</SelectContent></Select>;}
export function Badge({value}:{value:string}){return <span className={`badge ${value.toLowerCase().replaceAll(' ','-')}`}>{value}</span>;}
export function Stats({reports,admin=false}:{reports:Report[];admin?:boolean}){return <div className="stats">{[{label:admin?'Total issues':'Total reports',value:reports.length,icon:FileText,color:'blue',note:'Every report makes a difference'},{label:'Pending',value:reports.filter(r=>r.status==='Pending').length,icon:Clock3,color:'amber',note:'Awaiting a team review'},{label:'In progress',value:reports.filter(r=>r.status==='In Progress').length,icon:LoaderCircle,color:'purple',note:'Our teams are on it'},{label:'Resolved',value:reports.filter(r=>r.status==='Resolved').length,icon:CheckCircle2,color:'green',note:'A little better, together'}].map(s=><div className="stat glass" key={s.label}><div className="stat-top"><span>{s.label}</span><span className={`icon-box ${s.color}`}><s.icon size={19}/></span></div><strong>{s.value.toString().padStart(2,'0')}</strong><small>{s.note}</small></div>)}</div>;}
export function date(value:string){return new Date(value).toLocaleDateString('en-GB',{day:'numeric',month:'short',year:'numeric'});}
