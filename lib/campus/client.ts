export async function request(url:string,options:RequestInit={},timeoutMs=15000){const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),timeoutMs);try{return await fetch(url,{...options,signal:controller.signal,credentials:'same-origin',cache:'no-store'});}catch(e){if(e instanceof Error&&e.name==='AbortError')throw new Error('The connection is taking too long. Please try again.');throw new Error('Connection interrupted. Check your internet and try again.');}finally{clearTimeout(timer);}}
export function reportRequestId(){if(typeof crypto.randomUUID==='function')return crypto.randomUUID();const b=crypto.getRandomValues(new Uint8Array(16));b[6]=(b[6]&15)|64;b[8]=(b[8]&63)|128;const h=Array.from(b,x=>x.toString(16).padStart(2,'0')).join('');return `${h.slice(0,8)}-${h.slice(8,12)}-${h.slice(12,16)}-${h.slice(16,20)}-${h.slice(20)}`;}
